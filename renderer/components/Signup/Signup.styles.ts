import styled from 'styled-components';
import {
  LoginButton,
  LoginGuideText,
  LoginInput,
  LoginSignupText,
  LoginTextBox,
} from '../Login/Login.style';

export const SignupInput = styled(LoginInput)``;

export const SignupButton = styled(LoginButton)``;

export const SignupTextBox = styled(LoginTextBox)``;

export const SignupGuideText = styled(LoginGuideText)``;

export const SignupText = styled(LoginSignupText)``;
