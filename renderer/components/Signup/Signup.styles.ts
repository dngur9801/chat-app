import styled from 'styled-components';
import { LoginButton, LoginInput } from '../Login/Login.style';

export const SignupInput = styled(LoginInput)``;

export const SignupButton = styled(LoginButton)``;
